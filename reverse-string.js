let sentence = 'I am from RanirBandar';
console.log(sentence);

let reverse = '';
for(let section of sentence){  //takes every character of a string
    reverse = section + reverse;
}
console.log(reverse);
